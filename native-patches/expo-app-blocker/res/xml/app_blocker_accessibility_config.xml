<?xml version="1.0" encoding="utf-8"?>
<!--
  Optional fast path for Prayer Lock. When enabled in system settings, fires on
  every window change so we can re-block immediately after an app's built-in
  fingerprint/PIN screen (WhatsApp, Telegram, etc.) without waiting for the
  UsageStats poll loop.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
  android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
  android:accessibilityFeedbackType="feedbackGeneric"
  android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows"
  android:canRetrieveWindowContent="false"
  android:notificationTimeout="100"
  android:description="@string/app_blocker_accessibility_description" />
